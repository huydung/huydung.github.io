GAME-PROTO IMAGES — screenshot manifest
========================================
Drop your screenshots into this folder using the exact filenames below.
The guide will show them automatically; placeholders appear until the file exists.

STEP 0 — Set up your tools
  agent-interface.png       → Screenshot of Claude/Codex/Gemini agent desktop app

STEP 1 — Find the idea
  brainstorm-output.png     → Example brainstorm output with numbered ideas + ratings

STEP 2 — Lock the design / GDD
  gdd-example.png           → Example gdd.md rendered in VS Code or similar
  wireframe-example.png     → Wireframe of main game screen (SVG or image)

STEP 3 — Write the TDD
  config-file.png           → Example src/config.js with comments

STEP 4 — Build plan / milestones
  plan-md.png               → Example plan.md with milestone scope + test checklist

STEP 5 — Build
  agent-mid-session.png     → Agent running mid-build (file tree + terminal visible)
  game-in-browser.png       → Finished prototype running in the browser

STEP 6 — Fix bugs
  devtools-console.png      → Browser DevTools Console showing error log

STEP 7 — Iterate
  feature-before-after.png  → Side-by-side before/after of a new feature added

STEP 8 — Ship
  github-pages-settings.png → GitHub repo → Settings → Pages screen
  live-prototype-phone.png  → Live URL open on a real phone
